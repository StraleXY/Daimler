import { UrlSentizerPipe } from './url-sentizer.pipe';

describe('UrlSentizerPipe', () => {
  it('create an instance', () => {
    //const pipe = new UrlSentizerPipe();
    //expect(pipe).toBeTruthy();
  });
});
